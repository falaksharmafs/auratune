# Core DSP / perception
numpy
scipy
librosa
soundfile

# Optional: real stem separation (falls back to librosa hpss if unavailable)
torch
demucs

# Agent pipeline
langgraph
langchain-core
anthropic   # optional: command parsing, explanations, EQ-screenshot reading

# EQ-screenshot reading also supports Google Gemini (free tier) via a plain
# REST call -- no extra package needed, just set GEMINI_API_KEY.

# Data layer (optional -- falls back to local JSON if unset/unreachable)
pymongo

# Local ML: genre/mood classifier (ml/train.py, perception/genre_classifier.py)
# Optional -- if ml/models/ hasn't been generated yet, the Genre agent is a
# clean no-op and the rest of the pipeline is unaffected.
scikit-learn
joblib
# torch: CPU build is enough (pip install torch); ml/train.py uses CUDA
# automatically if a GPU + CUDA build of torch is available.

# UI
streamlit
plotly
matplotlib

# Live mic capture (optional, for real deployment instead of synth scenarios)
sounddevice
streamlit-webrtc
